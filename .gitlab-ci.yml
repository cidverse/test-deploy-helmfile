
# cid-workflow-version: 0.9.0

# This file is generated by the CID Workflow GitLab App.
# DO NOT EDIT!

# global defaults
default:
  image: quay.io/podman/stable@sha256:42d8b3d789333f07e631305325a02b4ab33c3f107413e04b612973a72f1d7deb # v5.8.1-immutable
  before_script:
    - bash .cid/scripts/install.sh "0.9.0" ""

# variables
variables:
  # git settings
  GIT_STRATEGY: clone
  GIT_DEPTH: 10

# stages
stages:
    - lint
    - publish
    - deploy

# configuration snippets
.default-job: &default_job
  
  tags:
    - saas-linux-small-amd64
  allow_failure: false
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - api_failure
      - scheduler_failure

# Nightly
## helmfile-lint [deployment-helmfile-dev]
"nightly/helmfile-lint [deployment-helmfile-dev]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "helmfile-lint-deployment-helmfile-dev"
    artifacts:
      name: "nightly/helmfile-lint [deployment-helmfile-dev]"
      paths:
        - .dist/helmfile-lint-deployment-helmfile-dev/
      expire_in: 1 day

# Pull Request
## helmfile-lint [deployment-helmfile-dev]
"pull-request/helmfile-lint [deployment-helmfile-dev]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "helmfile-lint-deployment-helmfile-dev"
    artifacts:
      name: "pull-request/helmfile-lint [deployment-helmfile-dev]"
      paths:
        - .dist/helmfile-lint-deployment-helmfile-dev/
      expire_in: 1 day

# Release
## changelog-generate
"release/changelog-generate":
    <<: *default_job
    stage: publish
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "changelog-generate"
    artifacts:
      name: "release/changelog-generate"
      paths:
        - .dist/changelog-generate/
      expire_in: 1 day
## gitlab-release-publish
"release/gitlab-release-publish":
    <<: *default_job
    stage: publish
    needs: ["release/changelog-generate"]
    dependencies: ["release/changelog-generate"]
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitlab-release-publish"
    artifacts:
      name: "release/gitlab-release-publish"
      paths:
        - .dist/gitlab-release-publish/
      expire_in: 1 day
## helmfile-deploy [deployment-helmfile-dev]
"release/helmfile-deploy [deployment-helmfile-dev]":
    <<: *default_job
    stage: deploy
    needs: ["release/changelog-generate", "release/gitlab-release-publish"]
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "helmfile-deploy-deployment-helmfile-dev"
    artifacts:
      name: "release/helmfile-deploy [deployment-helmfile-dev]"
      paths:
        - .dist/helmfile-deploy-deployment-helmfile-dev/
      expire_in: 1 day

# Main
## helmfile-lint [deployment-helmfile-dev]
"main/helmfile-lint [deployment-helmfile-dev]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "helmfile-lint-deployment-helmfile-dev"
    artifacts:
      name: "main/helmfile-lint [deployment-helmfile-dev]"
      paths:
        - .dist/helmfile-lint-deployment-helmfile-dev/
      expire_in: 1 day
